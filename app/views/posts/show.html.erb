<div class="col-md-12">
  <div class="post">
    <%= link_to("Edit", edit_group_path(@posts), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @posts.title %></h2>
  <p><%= @posts.description %></p>
</div>
