<div class="col-md-12">
  <div class="posts">
    <%= link_to("Edit", edit_group_post_path(@posts), class: "btn btn-primary pull-right")%>
    <%= link_to("Delete", edit_group_post_path(@posts), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @posts.title %></h2>
  <p><%= @posts.description %></p>
</div>
