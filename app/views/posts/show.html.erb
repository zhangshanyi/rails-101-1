<div class="col-md-12">
  <div class="post">
    <%= link_to("Edit", edit_group_post_path(@post), class: "btn btn-primary pull-right")%>
    <%= link_to("Delete", edit_group_post_path(@post), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @post.title %></h2>
  <p><%= @post.description %></p>
</div>
